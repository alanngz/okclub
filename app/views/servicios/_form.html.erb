<%= form_for(@servicio) do |f| %>
  <% if @servicio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@servicio.errors.count, "error") %> prohibited this servicio from being saved:</h2>

      <ul>
      <% @servicio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% prove = {} %>
  <% Proveedor.all.each do |proveedor| %>
    <% prove[proveedor.nombre] = proveedor.id %>
  <% end %>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :proveedor_id %><br>
    <%= f.select :proveedor_id, prove %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
